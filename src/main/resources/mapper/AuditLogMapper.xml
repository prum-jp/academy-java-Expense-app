<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.expenseapp.mapper.AuditLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO audit_logs (user_id, action, target_table, target_id, created_at)
        VALUES (#{userId}, #{action}, #{targetTable}, #{targetId}, #{createdAt})
    </insert>
</mapper>
